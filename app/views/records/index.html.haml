.wrapper
  = render "top/side_bar"
  .main__recdord
    .main__redord__contents
      作業累計時間
      .record_time
        - if @time_min
          - if @time_min > 0
            = "#{@time_min}分"
          - else
            まだ０分未満です。
          
        - else
          まだ記録はありません。
          