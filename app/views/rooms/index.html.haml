.wrapper
  = render "top/side_bar"
  .main__rooms
    .main__rooms__contents
      .rooms__msg
        %h1 部屋を選んでね。
      .room_lists
        %ul.rooms
          - @rooms.each do |room|
            %li.room
              = link_to "#{room.name}", room_path(room) ,class: "side_bar__content__"
            